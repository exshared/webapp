(function(){
    'use strict';

    angular
        .module('exsharer.auth', ['ui.router']);
        .config(config)
        .run(run);

    function config(){

    };

    function run(){

    };
    
})();
